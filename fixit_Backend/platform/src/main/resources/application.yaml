spring:
  application:
    name: platform

  datasource:
    url : jdbc:postgresql://localhost:5432/fixit_db
    username: postgres
    password: psql1234

  jpa:
    hibernate:
      ddl-auto:  update
    show-sql: true
    properties:
      hibernate :
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: AzuoRrtS7H5u4dJK/LKhODI7TXs9NfqX+SAN48EEcLo=

  springdoc:
    api-docs:
      path: /v3/api-docs

    swagger-ui:
      path: /swagger-ui.html
      operations-sorter: method
      tags-sorter: alpha
      persist-authorization: true
      disable-swagger-default-url: true

app:
  auth:
    cookie-name: fixit_access_token
    cookie-max-age: 3600
    cookie-secure: false
    cookie-same-site: Lax

frontend:
  origin: http://localhost:5173